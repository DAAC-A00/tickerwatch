// common_setting.dart

class CommonSetting {
  bool isLightMode;
  bool isDevMode;
  bool isSuperMode;

  CommonSetting({
    required this.isLightMode,
    required this.isDevMode,
    required this.isSuperMode,
  });
}
