// common_setting.dart

class CommonSetting {
  bool isLightMode;
  bool isAdminMode;
  bool isSuperMode;

  CommonSetting({
    required this.isLightMode,
    required this.isAdminMode,
    required this.isSuperMode,
  });
}
